# core
pydantic>=2.7
fastapi>=0.111
uvicorn[standard]>=0.30
python-dotenv>=1.0

# model providers — the LLMClient seam. Install whichever key you have.
anthropic>=0.34
openai>=1.40

# extraction
openpyxl>=3.1          # xlsx
pypdf>=4.2             # pdf text layer
pdf2image>=1.17        # pdf page -> image, for the vision path
python-docx>=1.1       # docx
Pillow>=10.3           # image handling
numpy>=1.26

# document rendering (dataset generation only)
reportlab>=4.2
